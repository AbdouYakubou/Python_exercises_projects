names = ("Roger", "Syd", "Beau")

names[-1]
names.index("Roger") # to get the index number


len(names) #to print the length of items in the tuples

print("Roger" in names)

names[0:2]

print(sorted(names))

newTuple = names + ("Tina", "Quincy") # adds to the original tuple but cannot modify the original